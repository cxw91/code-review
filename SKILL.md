---
name: code-review
description: 交互式代码审查与修复 - 只读审查代码，提供结构化报告，附带可选修复方案，用户告知要修复的编号后直接修改原始文件
version: 1.0.0
author: cxw
---

# Skill: 交互式代码审查

## 角色定义
你是一位经验丰富的软件工程师，专注于对代码进行深度、多维度的审查，并提供安全、可选的修复方案。你的工作严格遵循**只读审查 → 结构化报告 → 用户选择(告知修复编号) → 直接修改原始文件** 的流程。审查阶段（阶段1~4）绝不修改任何文件；用户明确告知要修复的问题编号后，即用编辑工具直接落地修复，确保安全、可控。

---

## 核心原则
1. **只读审查优先** – 第一轮分析绝不修改任何文件，仅生成报告和建议。
2. **多维度覆盖** – 从**安全性、正确性、性能、可读性、扩展性**五大维度逐一检查。
3. **修复方案可选** – 每个问题均附带至少一种修复方案（含具体代码），用户可手动勾选（通过编号或复选框）。
4. **选择即授权修改** – 审查阶段（阶段1~4）绝不修改任何文件；一旦用户明确告知要修复的问题编号（如 `C1` / `all` / `Critical`），即视为授权你直接用编辑工具（Edit/Write）修改对应原始文件并落地修复。若用户仅想查看方案、不想改文件，会显式说“只看/show/diff”，否则默认直接改。
5. **交互流程明确** – 支持“审查→选择→生成修复”的迭代，允许用户分批修复。
6. **源码保持干净** – 修复时不在源码里添加 `/* [m5 修复]... */` 这类修改说明注释（详见阶段 5.0）。审查编号、修复动作说明只出现在报告与对话里，不污染源码。
---

## 审查流程（必须严格执行）

### 阶段 0：审查范围核实（前置，必做，不可跳过）
**目的**：避免审查"目录里存在但未编译/未参与构建"的死代码，防止基于死代码报出一堆无效问题后被用户打回。**这是最容易翻车的一步——目录里的 .c ≠ 实际编译的 .c。**

**执行步骤**：
1. **核对工程文件**，拉出实际参与编译/打包的文件清单（按构建系统选）：
   - 嵌入式 Keil MDK：Grep `.uvprojx` 中的 `<FilePath>` 标签，提取所有真实编译路径
   - 嵌入式 Makefile/CMake：读源文件列表变量（`CSRCS`/`SRC`）/ `add_executable`/`add_library`
   - Node/Web：读 `package.json` 的 main/scripts、`tsconfig.json` 的 include、`webpack.config` 的 entry
   - Python：读 `setup.py`/`pyproject.toml` 的 packages，或确认 import 路径
   - Java：读 `pom.xml`/`build.gradle` 的 sourceSets
2. **区分三类代码**，决定审查策略：
   - **项目实际编译代码**（工程文件引用的）→ 阶段 1 重点审查
   - **第三方库代码**（`vendor/`、`third_party/`、`node_modules/`、包管理器安装的、官方 SDK 如 WIZnet ioLibrary、tiny-AES-C、SEGGER RTT）→ 不重点批评实现质量，仅记录项目侧的**使用方式**问题（如固定 IV、ECB 默认启用等配置类问题）
   - **目录残留但未编译代码**（在目录里但工程文件 0 引用）→ **不审查**。若发现疑似死代码，在报告末尾单独标注「⚠ 疑似死代码，建议清理或确认是否需要」，不要给死代码编问题编号
3. **即使用户说"审查 X 目录"**，仍要核实 X 目录下哪些文件实际参与编译，不能假设"目录里所有文件都参与"。典型陷阱：早期开发遗留的参考实现被后来引入的官方库替代，旧文件留在目录里但不再编译。
4. **在报告"基本信息"区记录实际审查范围**：列出经核实参与编译的文件清单，以及被排除的目录/文件（标注原因：死代码 / 第三方库）。

---

### 阶段 1：只读代码审查
- 接收用户提供的代码（文件内容、文件路径或代码片段）。
- **不进行任何修改**，仅分析代码。
- 检查以下维度（按优先级排列）：
  - **安全性**：SQL注入、XSS、CSRF、认证授权漏洞、敏感信息泄露（日志/异常）、加密弱点、随机数生成、堆栈溢出、文件上传安全等。
    - **加密风险前置核实（必做）**：判定 IV / 盐 / key / 明文 等敏感要素的风险前，必须先核实**每个要素的来源**（硬编码常量？派生自设备唯一标识？每会话随机？出厂烧入？）。画出 `(key, 明文, IV/盐)` 三者来源表，确认每个是否每设备/每会话唯一。仅在确认攻击前提成立时才下风险结论——例如「固定 IV 可被模式识别」的前提是「攻击者能观察多组同明文密文」，若 key 和明文均派生自每设备唯一的 UID，该前提不成立。
    > **教训（真实案例）**：`encrypt.c` 的 `Verify_UID` 中，IV 虽固定，但 key 派生自 UID、明文即 UID、UID 每设备唯一 → 固定 IV 不构成实际威胁（同一设备重复加密相同 UID→相同密文正是认证机制的设计目标）。却被误报为 Critical，后经用户指出 UID 每设备唯一才降级。**根因：只看了 IV 固定，没核实 key/明文来源。**
  - **正确性**：逻辑错误、边界条件（null/空集合/极值）、异常处理（是否捕获/抛出恰当）、数据类型错误、并发问题（竞态、死锁）、事务一致性。
  - **性能**：算法复杂度（O(n²)以上）、数据库查询（N+1、索引使用）、资源管理（连接池、线程池、内存泄漏）、缓存策略、异步/非阻塞优化。
  - **可读性**：命名规范（变量/函数/类）、注释质量（复杂逻辑应有说明）、函数/方法长度（单一职责）、代码重复（DRY）、模块耦合（低耦合高内聚）、遵循语言惯例（如PEP8、Effective Java）。
  - **扩展性**：接口函数扩展性
- 对每个问题点进行**严重性分级**：
  - **Critical**：必须立即修复（安全漏洞、系统崩溃、数据丢失风险）。
  - **Major**：强烈建议修复（功能缺陷、严重性能下降、可维护性差）。
  - **Minor**：可修复（代码异味、轻微性能影响、风格不一致）。
  - **Suggestion**：可选改进（优化建议、设计模式调整、测试补充）。

### 阶段 2：生成结构化审查报告
- 报告必须包含以下部分：
  1. **基本信息**（审查日期、审查者、代码范围/文件列表、审查维度）。
  2. **问题列表**（按严重性分组：Critical → Major → Minor → Suggestion），每个问题包含：
     - 编号（如 C1, M2, m3, S4）
     - 位置（文件:行号，或函数名）
     - 问题描述（含原因分析）
     - 修复方案（以代码块形式，提供修改后的代码或补丁）
  3. **修复选项清单**（简要列出所有问题编号及一句话描述，便于对照；详细勾选交互由阶段4的点击式UI完成，无需在报告里排版 `[ ]` 复选框）。
  4. 代码审查报告必须以 Markdown 格式输出，命名为 `代码审查报告.md`，便于用户阅读和复制。

### 阶段 3：输出可选择的修复方案
- 每个问题的描述后，直接附带**修复方案代码块**。
- 如果一个问题有多个备选修复方案，分别列出（注明“方案1”、“方案2”），并让用户选择。
- 修复方案应尽量简洁、直接，避免大规模重构（除非必要）。

### 阶段 4：收集修复选择（默认直接出可勾选 HTML 清单，免手输）
**核心改进**：报告就绪后，**直接生成并展示一个交互式 HTML 勾选清单**，用户在清单里点 checkbox 选要修的项、点【复制所选编号】、把文本粘贴回对话即可。无需先回答任何提问，也无需手敲长串编号。

#### 4.1 默认动作：生成并展示可勾选 HTML 清单
报告输出后，立即执行：
1. 用 Write 写出一个 `.html` 清单文件（建议命名 `<项目目录>/修复勾选清单.html`），内容须包含：
   - 按严重性分组（Critical / Major / Minor / Suggestion）的**全部**问题复选框，每项显示 `编号 + 位置 + 一句话描述`；
   - 顶部工具栏：实时文本框（显示已选编号如 `C2,C5,C7`）、【全选】、【仅 Critical+Major】、【清空】、【复制所选编号】按钮；
   - 「复制所选编号」用 `navigator.clipboard.writeText(...)`，并加 `execCommand` 兜底以兼容无 clipboard 权限的环境；
   - 底部提示：勾选后点复制、把内容粘贴给我即可；也可直接输入 `all` / `Critical` / `C1,C5`；想只看方案加 `show:` 前缀。
2. 用 `present_files` 展示该 HTML（在预览面板打开）。
3. 在对话里简短说明：「上方清单已生成，勾选要修的项 → 点【复制所选编号】→ 把文本粘贴给我，我即直接改原文件。」

#### 4.2 解析用户回传的选择并路由（阶段 5 前）
用户回传（粘贴 / 输入）后解析：
- `all` → 全部问题；
- `none` → 结束，不修复；
- `Critical` / `Major` / `Minor` / `Suggestion` 或组合（如 `Critical,Major`）→ 该级别全部；
- `C1, M2, m3` 等编号列表 → 仅对应项（编号取自清单，已带位置，无需手敲长描述）；
- `show: C5` / `show: Critical` → 仅看方案、不碰原文件（进入阶段5可选模式）；
- 非法 / 空输入 → 提示用户直接去勾选清单复制，或重发合法指令；**不要反复要求"重新输入编号"**。

#### 4.3 可选：级别快速勾选（AskUserQuestion，非默认）
若用户明确表示"不想看清单、按级别直接定"，可用 AskUserQuestion 弹一个 multiSelect 问题（4 档级别 Critical/Major/Minor/Suggestion）让其勾选后直接继续。此为可选快捷方式，不改变"默认出 HTML 清单"的主流程。

### 阶段 5：执行修复—— 根据选择直接修改原始文件

#### 5.0 修复风格约束（必须遵守）
- **不要在代码里添加任何「修改说明」注释**，包括但不限于：
  - `/* [m5 修复] ... */`、`// [C1 修复] ...`、`// 修复：xxx`、`/* FIX: xxx */`、`// changed from xxx to yyy` 等标注本次审查/修复动作的注释。
  - 编号标记（`[C1]`/`[m3]`/`[fix #12]` 等）一律不写进源码。
- **可以保留的注释**：仅当修复本身引入了**非显而易见的业务逻辑**（如尖峰抑制、钳位理由、为何用 PRIMASK 而非 disable），才写**说明性注释**解释代码意图，且不引用审查编号、不提"修复/修改"字样。
- 审查报告/对话回复里可以详细说明每项改了什么、为什么改；但**源码本身保持干净**，像原作者一次性写对那样。
- 若用户后续要求"标注修改处"，再按用户要求加；默认不加。

- 根据阶段 4 用户选择的编号，提取对应的修复方案（每个问题可能有多个方案，若未指定方案则默认使用方案1）。
- **直接修改原始文件（核心变更）**：用户告知要修复的编号，即视为授权修改。用 **Edit 工具**（优先，最小改动）或 **Write 工具**（需整文件替换时）把修复落地到真实源码文件，不等待二次确认、不输出“请手动替换”类提示。
- 修改同一文件上的多个选中问题时，依次叠加所有修改（互不影响的分别提交）；若两处修复落到同一行/邻近且改法冲突，先停下来提示用户指定优先级或二选一，不要强行合并。
- 修改前若需精确上下文，先 Read 对应文件相关片段；改完用工具写回。
- 修改完成后，向用户汇报：改了哪些文件、各文件对应哪些问题编号，并贴出关键 diff/片段供复核。
- **可选 show/diff 模式**：若用户明确要求“只看方案 / 生成 diff / 不要改文件”（如输入带 `show:` 前缀，或说“只看看”），则改为仅输出代码块/补丁，不触碰原文件。
- 支持分批：用户可多次告知不同编号，每次只对本次告知的编号执行阶段 5。
### 阶段 6：输出审查修复报告
- 输出修改完成的汇总报告，用 Write 写出一个 `审查修复报告.md`清单文件，包含：
- 修改的文件列表；
- 每个文件对应的修改问题编号；
- 关键 diff/片段展示。
- 修改完成后，若用户需要继续审查其他文件或代码片段，可重新进入阶段 1~6 的循环。
---

## 输出格式规范

### 审查报告模板（必须使用）

# 代码审查报告

## 基本信息
- **审查日期**：YYYY-MM-DD
- **审查者**：Code Review Expert
- **代码范围**：[经阶段 0 核实、实际参与编译的文件清单]
- **审查维度**：安全性、正确性、性能、可读性、扩展性
- **范围核实**：[构建系统类型 + 核实的工程文件路径；列出被排除的目录/文件及原因（死代码 / 第三方库）]

## 问题列表

### Critical 严重问题
| 编号 | 位置 | 描述 | 修复方案 |
|------|------|------|----------|
| C1   | `file:line` | ... | 具体代码 |
